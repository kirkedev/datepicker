{
  "name": "datepicker",
  "version": "1.0.0",
  "description": "An example datepicker project",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf src/**/*.{js,d.ts,js.map}",
    "lint": "eslint src/**/*.ts --fix",
    "pretest": "yarn run clean && yarn run lint",
    "test": "jest",
    "prebuild": "yarn test",
    "build": "tsc && rollup -c rollup.config.js",
    "postbuild": "yarn run clean"
  },
  "author": "Andrew Kirkegaard",
  "license": "ISC",
  "devDependencies": {
    "@ampproject/rollup-plugin-closure-compiler": "^0.9.0",
    "@types/jest": "^24.0.15",
    "@typescript-eslint/eslint-plugin": "^1.11.0",
    "@typescript-eslint/parser": "^1.11.0",
    "eslint": "^5.0.0",
    "http-server": "^0.11.1",
    "jest": "^24.8.0",
    "rollup": "^1.16.6",
    "rollup-plugin-commonjs": "^10.0.1",
    "rollup-plugin-node-resolve": "^5.2.0",
    "ts-jest": "^24.0.2",
    "typescript": "^3.5.1"
  },
  "private": true,
  "workspaces": [
    "src/itertools",
    "src/dates",
    "src/react",
    "src/d3"
  ],
  "dependencies": {
    "tslib": "^1.10.0"
  }
}
